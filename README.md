# Women Registration Program — React + Node API

- **Frontend:** Vite + React (`npm run dev`, `npm run build`)
- **API:** `server/` — Express + PostgreSQL (`cd server && npm run dev`)

Deploy on **Render** using `render.yaml` (see `RENDER_DEPLOY.md`).
